#ifndef _expand_h
#define _expand_h
    #include <iostream>
    #include <vector>

    using namespace std;

    class expand{
    private:
    public:
        expand() = default;  
        vector <char> expand_function(vector <char> expand_arr); //拓展名处理
    };

#endif